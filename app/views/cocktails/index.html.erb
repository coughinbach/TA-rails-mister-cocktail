<% @cocktails.each do |cocktail| %>
  <%= link_to cocktail.name, cocktail_path(cocktail.id) %>
  <%= link_to cocktail_path(cocktail), method: :delete, data: { confirm: "Remove #{cocktail.name}?" } do %>
      remove
   <% end %>
<% end %>

<%= link_to "add new cocktail", new_cocktail_path %>
