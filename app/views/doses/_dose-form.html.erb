<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, label: "amount", placeholder: "how much does the recipe require?" %>
  <%= f.association :ingredient, label: "ingredient" %>
  <%= f.submit "add to recipe", class: "btn btn-form" %>
<% end %>
